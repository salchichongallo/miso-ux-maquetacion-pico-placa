import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ux.alarmas.picoplaca',
  appName: 'Alarmas Pico Placa',
  webDir: 'dist/mobile',
};

export default config;
